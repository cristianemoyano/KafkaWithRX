### kafka-python with ReactiveX

Testing kafka with reactive programming.


#### Start services

`$ docker-compose up`


#### Start services

`$ docker-compose up`


#### Shell container

`$ docker container ls`

`$ docker exec -ti <container SHA> /bin/bash`


#### Environment

`$ pipenv shell`

`$ pipenv install`
